# 08 · 性能与体验

源：`docs/aiui/0-guide/performance/*`

- **启动性能**：减小代码包体积/移除无用资源；**分包加载**(拆多个分包按需载)；**预拉取**核心数据。
- **运行时性能**：避免频繁 `setData`(合并多次更新为一次)；减少数据传输量(只传视图层需要的，勿传大对象)；列表优化(长列表组件复用 DOM 节点)。
- **诊断工具**：DevTools 的 **Performance 面板**记录分析 FPS、内存、`setData` 耗时等，定位瓶颈。
