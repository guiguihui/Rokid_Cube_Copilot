# 测试图片，不提交
testImg/

# jsar-project/AIUI 的本地克隆（参考用，自带 .git，需要时自行 clone）
AIUI/

# 本地个人配置
.claude/settings.local.json

node_modules/

# adb 截屏临时文件
_shot.png

# PC 端开发/基准/分析工具 + 测试图片帧（纯本地，不打包进眼镜、不上传 GitHub）
# 历史已用 git filter-repo 抹除；本地文件仍保留供开发，但整目录不再纳入版本控制
tools/
