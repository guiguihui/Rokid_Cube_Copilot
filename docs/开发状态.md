# 魔方还原助手 · 开发状态日志

> 用途：记录每次开发改了什么、为什么改、当前状态与待办，供后续回看。
> 规则：**最新在最上面**。每次开发追加一个 `## [日期] 标题` 区块，按下面模板写。
> 相关文档：产品设计 `魔方还原助手_MVP_产品设计.md` · 开发方案 `魔方还原助手_开发实现方案.md`。

---

## 当前状态速览（每次开发后更新这一节）

- **当前版本**：**1.0.49**（显示在首页右上角；改代码即递增 `pages/input/index.ink` 的 `VERSION`，并在回复里告知用户）。
- **真机自动化测试**：可纯 PC 用 adb 部署+驱动+观察，流程见 `docs/真机自动化测试流程.md`。
- **项目根**：`E:\ROKIT` 本身就是 AIUI agent（应用已提升到仓库根，Craft 以 ROKIT 为工作区即运行本应用）。
- **入口**：`app.json` → `pages/input/index`（输入页）→ `pages/scan/index`（扫描页）→ `pages/guide/index`（引导页）。
- **求解器**：`min2phase`（Kociemba 两阶段近最优 ~20 步），见 `lib/vendor/min2phase.js`；剪枝表首次求解一次性建好(端侧 1–4s，`warmup` 可预热)。**已弃用 CFOP(rubiks-cube-solver, 50–130 步) 与 cubejs(运行时建表冻屏)**。
- **扫描**：`lib/colors.js`(RGB→HSV→6色) + `pages/scan`(camera+引导框+确认拍照+逐面识别→网格存 `cube.scannedGrid`→回输入页校正)。阈值集中在 `colors.js#CONFIG`。
- **图像解码**：takePhoto 返回格式因环境而异——真机 webp，Craft webcam 截图为 png/jpeg。`pages/scan` 按 magic 字节分支：webp→`lib/webp.js`、png→`lib/png.js`(自写+`vendor/tiny-inflate.js`)、jpeg→`lib/jpeg.js`(+`vendor/jpeg-decoder.js`)。三种格式均 node 验证通过。
- **交互**：硬件仅 **向前/向后/单击/双击** → 单轴线性导航（向前/向后逐项移动 + 单击确认；所有动作都是可选项，不依赖双击）。代码里 forward=ArrowDown/Right、back=ArrowUp/Left、confirm=Enter/Space/NumpadEnter（真实 code 待 lastKey 调试确认）。各页有「上次按键」调试行。
- **首页(input)**：只展示魔方（两行各三面 U R F / D L B）+ 4 个按钮（扫描/载入打乱/重置/求解）线性导航；**不在首页改色**。
- **改色**：在扫描页 review 模式逐格编辑（向前/向后选格，单击切色；选「接受本面」进入下一面，「重拍」重来）。
- **测试方式**：Craft 浏览器「运行智能体」（摄像头用 Craft「摄像头」标签）；逻辑层用 node 离线测。AIUI CLI 本机未安装。
- **真机调试链路（已打通 2026-06-17）**：USB 数据线 + 眼镜调试模式 → **adb**（Android SDK 自带：`C:\Users\hhzheng\AppData\Local\Android\Sdk\platform-tools\adb.exe`，未进 PATH，用全路径或自行加；设备 `RG-glasses`/Android 12/SN `1901092524001700`，`adb devices`=`device`）。三件套：① **scrcpy** 实时投屏（winget `Genymobile.scrcpy` v4.0，眼镜屏 480×640，`FLAG_SECURE` 不挡）；② **logcat `Ink` 标签**看日志；③ `adb shell screencap` 单帧截屏。⚠️ Ink 运行时 = QuickJS+Skia 原生渲染、**不是 WebView** → `chrome://inspect` 对本应用无效；Ink 引擎 CDP 仅 `aiui run --debug` 开，语音调起的发布版不开。命令清单见下方 [2026-06-17] 真机调试链路打通条目。
- **已验证**：求解 selfTest/交叉验证 isSolved=true；颜色分类标准色 + 合成帧采样 PASS；全文件语法检查通过。
- **未在真机/真实相机验证**：扫描的真实识别准确率与阈值、语音命令、确认键实际 keycode。

### 已知待办 / 风险
- [ ] **真机/真实相机校准扫描阈值**（`colors.js#CONFIG`：白判定、色相分界）；用扫描页 debug 行(中心RGB/HSV)对照调。
- [ ] 真机验证确认键的 `event.code`（目前兼容 Enter/Space/NumpadEnter，若不符需适配）。
- [ ] 扫描的持握朝向与 facelet 行序一致性（当前按固定持握约定 + 中心强制，建议真机校验是否能解）。
- [ ] 层先法(LBL)分阶段教学（当前是 CFOP，步数偏多、非分阶段讲解）。
- [ ] 提示音资源 `assets/*.wav`（当前无音效）。
- [ ] 引导页箭头/HUD 在真机单绿屏的可读性微调。

---

## 模板（复制到最上面用）
```
## [YYYY-MM-DD] 简述本次改动
**背景/触发**：（为什么改，遇到什么问题）
**改了什么**：
- 文件 路径 — 具体改动
**为什么这么改**：（关键决策与权衡）
**验证**：（怎么验的，结果）
**遗留/下一步**：
```

---

## [2026-06-22] 仓库整理：README 更新 + tools/ 从历史抹除 + 只留 main 分支
**背景/触发**：用户要 ① 更新 GitHub README；② 测试图片/脚本不再上传；③ 理清分支。
**改了什么**：
- `README.md` — 重写为当前形态：min2phase 求解（Kociemba 近最优 ~20 步，弃 CFOP）、扫描页（locateCubeLocal 定位+判色复核）、唤醒词语音三页、硬件约束（QuickJS 无 Worker/WASM、相机仅 takePhoto、双击=系统弹栈、乐奇唤醒）、结构表更新（scan 页/voice.js/min2phase）。
- `tools/`（40 张 webp 测试帧 + ~37 个 .mjs 脚本 + 标注 JSON，~3.3MB）— **用 git filter-repo 从全部历史抹除**，本地文件保留供开发；`.gitignore` 改为整目录 `tools/` 排除。force-push 后 `.git` 4.7M→1MB。
- 分支 — 删除已并入 main 的 `添加语音功能`、`扫描魔方功能优化`（本地+远程），**只保留 `main`**。
**为什么这么改**：tools/ 纯 PC 开发/基准脚手架，不打包进眼镜（.aixignore）也不该进公开仓库；README 与代码现状脱节（仍写 CFOP/无扫描页/无语音）。
**验证**：`git log --all -- tools/` 为空（全史无残留）；`tools/` 文件仍在磁盘；main 已 force-push 且 tracking 正常；备份 `../ROKIT-pre-purge-backup.bundle`(4MB)。
**遗留/下一步**：唤醒词语音(v1.0.49)真机仍待用户验证（说「乐奇」是否触发 onVoiceWakeup 而非跳出系统助手）。注：本次无 app 代码变更，VERSION 不变(1.0.49)。

---

## [2026-06-19] v1.0.22/1.0.23 复核锁 + 判色算法重做(圆周色相)，6 张真机图判色 54/54
**背景/触发**：真机自动化拍摄时发现两坑：①一次确认=拍照+立刻"接受"翻面，复核屏停不住、且这次启动 console.log 不进 logcat → 加**复核锁**(v1.0.22：拍照后 1.5s 内忽略确认)。②AIUI 无文件写盘 API、发布版 run-as 拒读 → 帧拉不到 PC。于是用户提供 testImg/realImg 6 张真机图 + **人工真值标注**(tools/realimg-labels.json，已验证合法魔方：每色 9)。
**改了什么(v1.0.23 判色)**：
- `lib/colors.js` 新增 `classifyByAnchorsHue(rgb,anchors)`：**圆周色相相对分类**——白用相对饱和度门限(白锚与最暗彩锚饱和度中点)，彩色按**圆周色相距离**找最近锚。修了旧 `classifyByAnchors`(色度欧氏)**不懂红/橙跨 360/0 边界**(红≈358°/橙≈10°)导致的红橙混淆。
- `lib/colors.js` `sampleFace` 稳健采样：**中位数** + **丢弃"又暗又灰"像素**(黑色网格线/阴影，阈 sum<120 且 max-min<25)但**保留暗但高饱和的蓝** + 采样点**内缩 [0.2,0.5,0.8]**(避开斜面角格漂到边缘)。
- `pages/scan` finalize 改用 `classifyByAnchorsHue`。
**验证(PC, tools/eval.mjs 对真值)**：判色 **54/54=100%**（在"框定位正确"前提下；框由 tools/fit-box.mjs 用真值网格搜索得到，证明分类上限）。node 13/13 PASS。演进：相对色度24%→中位94%→内缩+红橙二判96%→**圆周色相98%→去黑灰采样100%**。
**没解决——自动定位**：tools/auto-locate.mjs 端到端(不用真值定位)：固定先验框 46%、自监督(饱和/网格线暗)搜索 24–31%。根因：realImg 是**手机照(3024×4032，非眼镜480×640)**、魔方**斜拿(透视)**、背后**笔记本屏在显示 app 彩色界面=强干扰**，轴对齐方框+简单目标定不住。训练出的位置先验：cx≈0.545 cy≈0.59(很稳) s≈0.16(变化大)。
**遗留/下一步**：①**判色已解决并入设备代码**，待真机部署 1.0.23 验证。②定位两条路：(A)产品用**引导握姿**(屏上对齐框，用户把魔方填满→定位由设计固定)+ 现 100% 分类器=推荐；(B)继续做真正的魔方检测器(透视四边形/网格线检测+抗屏幕干扰)=重活，且手机照带屏幕干扰是不代表真机的极端难例。③工具：eval/fit-box/auto-locate/cube-box/crop-zoom 均在 tools/。

## [2026-06-19] v1.0.21 在眼镜上做"先定位魔方面再识别" + 整帧调试视图
**背景/触发**：真机实拍复核屏发现固定采样框框住了**拇指+背景**（中心RGB(126,96,82)=肤色），判色全错。根因：无实时预览=盲拍，固定框跟不动魔方。**并且发现** `testImg/realImg` 那 6 张是**手机拍的桌面照**（笔记本屏幕上显示着 app 彩色界面=干扰物），**根本不是戴眼镜手持魔方的场景**——之前用它们标定固定框/调检测器是错样本。眼镜实拍是 480×640 webp、手持+房间背景，完全不同。
**改了什么**：
- `lib/colors.js` — 新增 `locateFace(rgba,w,h,opt)`：工作分辨率 W=120 下做积分图，搜"高饱和(s>0.28,v>0.20)+色相多样(≥3/8 个 30°桶)最密集"的方窗=魔方面（肤色单色相、低多样性被排除）；回全帧像素框 `{x0,y0,side,bins,fill,score}`，测不到返回 null。本地 ~16ms，估端侧 ~300ms（一次性）。
- `lib/colors.js` — `sampleFace` 增可选 `region` 参数（默认仍 `regionPx` 固定框）。
- `pages/scan` — `doCapture` 先 `locateFace`→采样用检测框、测不到退回固定框；`console.log('[scan] loc=…')` 打检测结果(进 logcat `ink_script::console`)。
- `pages/scan` — `drawReview` 临时改为**调试视图**：整帧 letterbox 进 240×240 + 红框(检测)+暗绿框(固定) 叠加，便于截屏核对定位。
**为什么这么改**：AIUI 无文件写盘 API（只有 per-agent 隔离的 localStorage、发布版 `run-as` 拒读），帧拉不到 PC；唯一观测=截屏。把"整帧+检测框"画到复核画布，截一张屏就能看到眼镜真实视角 + 检测器表现，是不靠文件 API 验证定位的最快路径。
**验证**：`node --test tests/` 13/13 PASS；`locateFace` 在 480×640 模拟帧 ~16ms 返回合理框；三文件已 push 到设备 runtime（VERSION/locateFace/调试文案/console.log 均 grep 确认）。**真机定位准确率待测**（需用户语音重启 1.0.21、扫描页拍照、停在复核屏，我截屏读红框位置）。
**遗留/下一步**：① 真机实拍验证红框是否套住魔方面、单色面会不会漏检（广角通常露多色，理论可检）；② 若稳定→把调试视图改回"裁剪到检测框"的生产复核视图；③ 检测器阈值(sMin/vMin/bins/fill)按真机实拍调；④ 旧固定框标定(0.45,0.55,0.30)是错样本所得，确认检测器可用后可弃。

## [2026-06-17] 真机调试链路打通（adb + scrcpy 投屏 + logcat），无代码改动
**背景/触发**：用户拿到数据线、眼镜已开调试模式，要在电脑上连真机调试，并希望像官方演示那样在电脑看眼镜实时画面。本次仅搭通真机回路，**未改任何代码**（用户要求）。
**关键发现/方法**：
- **设备已识别**：Windows PnP 见 `Android Composite ADB Interface`（`VID_18D1&PID_4EE7`）。`adb`（Android SDK 自带，路径见速览）`devices -l` → `product:glasses model:RG_glasses`，状态 `device`（已授权）。`aiui` CLI、`adb` 均未进 PATH（adb 本机其实已装）。
- **运行形态**：魔方 agent 跑在 `com.rokid.os.sprite.*` 进程内（前台 `SpriteMainActivity`），**无独立 Activity/APK**；Craft 打包→眼镜语音调起。
- **投屏**：`scrcpy`（winget 装 `Genymobile.scrcpy` v4.0）→ 实时镜像 480×640；`FLAG_SECURE` 未阻断（`adb shell screencap` 也能截到真实画面）。启动时 `set ADB=<上面的adb路径>` 让 scrcpy 复用同一 adb 避免版本冲突。
- **日志通道**：应用一切都进 logcat 的 **`Ink`** 标签——`ink_script::console`=应用 `console.log`（实测抓到 `[input] applyScannedGrid: none`）、`ink_framework::page`=页面生命周期+耗时、`ink_core::node`=模板告警、`AssistServer_Log`=按键事件。**chrome://inspect 不适用**（Ink 是 QuickJS+Skia 原生渲染，非 WebView；之前 logcat 里的 WebView 95 是支付宝的）。
- **命令清单**（`$adb` = 速览里的全路径）：
  - 看设备：`$adb devices -l`
  - 投屏：`$env:ADB=$adb; scrcpy --stay-awake`（`--record demo.mp4` 录屏、`--no-control` 只看不控）
  - 看自己的 log：`$adb logcat -v time | Select-String "ink_script::console"`
  - 全部相关日志（滤掉内核刷屏）：`$adb logcat -v time Ink:V AssistServer_Log:D *:S`
  - 单帧截屏：`$adb shell screencap -p /sdcard/s.png; $adb pull /sdcard/s.png`
**真机观察到的两条（仅记录，未改代码）**：
- **真实键码**：返回键 = `KEYCODE_BACK` / `keyName=Backspace`，且 `handledByInkGesture=true`（Ink 手势层已消费）。补充待办「确认键真实 keycode」的线索。
- **渲染告警**：`Template variable 'item.foc?'foc':''' / 'item.i' / 'item.id' is missing from data`——Ink 模板绑定**不支持三元/复杂表达式**，input/scan 焦点高亮的 `item.foc/.i/.id` 取不到值，需排查。
- **版本错位**：眼镜上跑的是 **v1.0.10**，代码已 1.0.13 → 需重新 Craft 打包部署。
**验证**：`adb devices`=device；`screencap` 截到真实魔方输入页；`scrcpy` `Texture: 480x640` 无报错出图；logcat 抓到应用 `console.log`。
**遗留/下一步**：① 重新打包部署 1.0.13；② 排查模板绑定 `item.foc/.i/.id` 告警（焦点高亮可能在真机失效）；③ 据真实键码（BACK=Backspace）核对 forward/back/confirm 映射。

## [2026-06-19] v1.0.20 标定固定采样框(偏左下) + 缩略图变清晰
**背景/触发**：真机 takePhoto=480×640 广角小图，魔方占比小、相机在左侧有视差→固定"中央66%框"框到背景(中心RGB≈白)，识别全错。用户提议：魔方落点基本固定→把框钉到那个位置。
**改了什么**：
- 标定：`tools/detect-cube.mjs` 限下半部分检测，跑 6 张 realImg→魔方稳定在 **中心(0.45W,0.55H)、边长0.30W**(偏左下，6张一致)。
- `lib/colors.js` — 新增 `SAMPLE_REGION`(帧分数)+`regionPx()`；`sampleFace` 改用标定区(不再中央66%)。
- `pages/scan/index.ink` — `drawReview` 裁到标定区 + N 40→**60**(更清晰)；`.guide` 九宫格盖满缩略图(=采样区)；aim 提示改"举到偏左下约占1/3"。VERSION→**1.0.20**。
**验证**：13/13 PASS；regionPx@480×640={x0:144,y0:280,side:144}；真照片采样落到魔方区(不再纯白背景)。
**遗留/诚实**：单张实测采样仍偏暗/糊——每张魔方位置有±0.05波动+斜拿透视，固定框只能"框到魔方区域"，精度靠"引导对准+缩略图重拍+相对锚色+手动改"兜底。下一步：真机引导握姿实测(realImg 是未引导更难的样本)。

## [2026-06-19] v1.0.19 W2 颜色识别改相对锚色（定位走引导框）
**背景/触发**：用户加了 6 张真机魔方照片(testImg/realImg)，要求识别分两步(先找魔方再识别)，且识别按"色块相对差异"而非绝对 RGB。
**实测结论（用真照片）**：① 魔方在杂乱广角里很小(~15%)、斜拿、有透视；② **自动定位不可靠**——饱和填充检测器框到手、色相多样性检测器框到笔记本屏幕(彩色代码+魔方UI=诱饵)。→ 与用户确认：**定位用引导框**(正对填满)，不做端侧自动检测。
**改了什么**：
- `lib/colors.js` — 新增 `classifyByAnchors(rgb, anchors)`：用本魔方 6 个中心块作锚色，按色度(去亮度)距离取最近——全相对、自适应光照。
- `pages/scan/index.ink` — 存每格原始 RGB(`detectedCells`)；`cycleCell` 标 `manual`；`finalize` 用 6 中心锚色统一识别(非中心、非手动格)，手动改过的保留。VERSION 1.0.18→**1.0.19**。
- `tests/classify.test.mjs` — 模拟白平衡+明暗+噪声：**锚色 avg 99.7%/worst 96.3% vs 绝对阈值 82.6%**。
- dev 工具：`tools/{pngw,analyze-real,detect-cube}.mjs`(CV 原型，含 node PNG 写出)；`.gitignore` 加 `tools/_*.png`。
**验证**：`node --test tests/` **13/13 PASS**；scan `node --check` 通过。
**遗留/下一步**：① 相对分类器真机实拍验证需"引导框正对捕获"(realImg 未引导不适合)；② T2.1 缩略图+T2.3 锚色识别上真机走查；③ W3 语音纠错。

## [2026-06-18] v1.0.18 真机自动化测试链路 + 测试图片离线化
**背景/触发**：用户问能否让我自己起窗口、自己拍照调试端到端，并写成可自执行流程文档。
**实测打通（纯 PC adb，无 Craft/无需戴）**：
- **部署**：`adb push` 到运行时目录 `/sdcard/jsai/runtime/617baca…_1.0.15_d471cf9b/`(可写)；目录名固定在上次 Craft 部署，靠 `agents_index.json` 找。
- **驱动**：`adb shell input keyevent`——`DPAD_*`→app `Arrow*`、`ENTER`→Enter、`BACK`→Backspace(实测「上次按键: ArrowRight」)。注意滑动 250ms 节流要 sleep。
- **观察**：`screencap`+pull(可直接读图)、logcat `Ink` 标签、`storage.json`(cube.moves)。
- **全自动 happy path 跑通**：首页→载入打乱→求解→引导页，得 `cube.moves="R L' D R2"`、引导页「1/4·右面 顺时针」+箭头。
- **架构发现**：agent 是 YodaOS 助手(`com.rokid.os.sprite.launcher` `SpriteMainActivity`)里的卡片，语音「打开魔方还原助手」调起；**从 feed 回到 agent 是 resume(onShow) 不重载 JS**——改代码后要"全新启动"(语音)才生效，这步暂需人。
**改了什么（代码）**：
- `lib/testData.js`(新增,34KB) — `tools/bake-testdata.mjs` 从 `testImg/*.png` 烘焙的 6 面 base64；`lib/testImages.js` 改为 `import { TEST_DATA }`(真机 `fetch` 不通→base64 离线优先)。`tools/bake-testdata.mjs` 新增。
- `docs/真机自动化测试流程.md`(新增) — 完整可自执行流程(前置/部署/重载/驱动/观察/现成脚本/限制)。
- `.gitignore` 加 `_shot.png`；VERSION 1.0.17→**1.0.18**。
**验证**：`node --test tests/` 12/12 PASS；node 验 `loadTestFrame('U')` 解 PNG+采样 OK；真机 happy path 截图确认。
**遗留/下一步**：① 自动化重载(语音注入/launch intent)仍缺——改代码后需语音重启；② 「测试图片」base64 版需下次全新启动后真机验；③ W2-T2.3 真机阈值校准；W3 语音纠错。

## [2026-06-18] v1.0.17 W2-T2.1 扫描快照复核（拍后看取景）
**背景/触发**：开 W2。无实时预览+相机在左侧→拍完不知道取景对不对。T2.1：拍照后把"刚拍到的画面"画出来供核对。
**改了什么**：
- `lib/colors.js` — 新增 `downsample(rgba,w,h,n,region?)`：n×n 色块均值缩略图，region 取中央正方形(=对齐 sampleFace 采样区/九宫格)。
- `pages/scan/index.ink` — review 模式 `<camera>`↔`<canvas id="review">` 按 `inReview` 切换；拍照后存 `this.lastFrame`，`drawReview()`(setTimeout 80ms 等画布就绪)用 `wx.createCanvasContext` 画 40×40 色块缩略图；HTML `.guide` 3×3 框叠其上=采样落点；camhint/statusText 改为"拍后核对取景"。VERSION 1.0.16→**1.0.17**。
- `tests/downsample.test.mjs` — 2 用例(尺寸 n×n、左红右蓝区域均值)。
**为什么这么改**：缩略图取中央正方形与预览 cover 裁剪一致→"框内"即采样区，用户能判断魔方是否落框、决定接受/重拍(T2.2 已有 accept/retake+逐格改色)。
**验证**：`node --test tests/` **12/12 PASS**；scan `node --check` 通过。真机 canvas 绘制/对齐待验。
**遗留/下一步**：T2.3 真机阈值校准(采样本→colors.test→调 CONFIG)；真机验 T2.1 缩略图绘制；W3 语音纠错。

## [2026-06-18] v1.0.16 T1.6-B 求解器预热（首解秒出）
**背景/触发**：推 T1.6-B——把 min2phase 一次性建表(~1–4s)挪到首页空档预热，让首解也无感。
**改了什么**：
- `lib/solver.js` — 加 `warmup()`(幂等，`solve(已还原态)`+`initFull()` 建满表)、`isWarmed()`。
- `pages/input/index.ink` — `onLoad` 后 `scheduleWarmup()`：`setTimeout(300ms)` 让首屏先画，再后台建表；建表期间显示「⏳ 求解器初始化中，稍候即可秒解…」(`warming` 态)。VERSION 1.0.15→**1.0.16**。
- `tests/solver.test.mjs` — 加用例 ⑧：warmup 幂等(首次 true、再次 false)、预热后求解仍正确。
**为什么这么改**：建表同步会冻屏，放首屏空档+提示比卡在求解时好；之后稳态求解 <1ms。
**验证**：`node --test tests/` **10/10 PASS**；input `node --check` 通过。
**遗留/下一步**：T1.5 真机量预热/首解实际冻屏体感（×10–40 估 1–4s，需真机确认是否够顺）；随后 W2 扫描快照复核 / W3 语音纠错。

## [2026-06-18] v1.0.15 文档审计对齐 + T1.6-A 求解中UI + 多测试打乱态
**背景/触发**：用户要求①审计代码 vs PRD/实现方案、说明偏离 ②所有改动落档 ③实现 A 方案(求解中 UI) ④载入测试打乱多增几个态。
**审计偏离（子 agent 独立核查，详见回复）**：
- 陈旧文档(已修)：PRD「颜色中文名」标 🚧待做，实际三页早已全中文(v1.0.13)→改 ✅；实现方案 §1 `cube.js`/`solver.js` 行仍写 `gridToRcs`/CFOP→改 `gridToFacelet`/min2phase；W1 头自相矛盾(说"下一步 T1.4a"却又标 ✅)→改。
- 代码有文档无(已补注)：载入测试打乱/重置、测试图片(testImages.js)、camhint 自诊断行、三页 debug 行(lastKey/焦点/RGB)→实现方案 §1 加「测试/调试辅助」说明；`vendor/cube-solver.js`+`gridToRcs` 死代码标注可删。
- 双击=返回上一级：**已真机确认全界面系统级生效**，App 无需处理双击（PRD 说法正确，不改代码）；`Backspace` 分支仅 PC/键盘调试兜底。
- 如实未做(文档已标待做，非偏离)：语音纠错(W3)、快照复核(T2.1)、语音菜单(W4)、提示音(W5)。
**改了什么（代码）**：
- `pages/input/index.ink` — **T1.6-A**：`doSolve` 把 `solve()` 推到 `setTimeout(…,60)`，先刷「⏳ 求解中…（首次需初始化，请稍候 1–4 秒）」再建表，消除首解"点了没反应"。VERSION 1.0.14→**1.0.15**。载入打乱 hint 提示"可换下一个，共6个"。
- `lib/cube.js` — 新增 `SCRAMBLE_GRIDS`(6 个 min2phase 生成的合法可解态，4/5/20/21/21/21 步，由易到难)。
- `lib/solver.js` — `scrambledGrid()` 改为**循环切** SCRAMBLE_GRIDS(每点一次换下一个)。
- `tools/gen-scrambles.mjs` — 新增(dev)，生成打乱态。
- `tests/solver.test.mjs` — 加用例 ⑦：SCRAMBLE_GRIDS 每个都 validate 合法 + 可解 1..25 步。
**验证**：`node --test tests/` **9/9 PASS**；input `<script setup>` `node --check` 通过。
**遗留/下一步**：T1.6-B 预热(pre-warm) + T1.5 真机量首解冻屏；双击映射待用户定；语音纠错/快照复核(W2/W3)。

## [2026-06-18] T1.4a 完成：求解内核换成 min2phase（19 步，TDD 全绿）
**背景/触发**：把近最优 min2phase 接进生产代码，替换 CFOP。TDD：先写测试再实现。
**改了什么**：
- `lib/cube.js` — 新增 `gridToFacelet(grid)`(颜色网格→URFDLB 面字母串)与逆函数 `faceletToGrid(s)`；保留 `gridToRcs`(旧 CFOP 兼容，已不用)。
- `lib/solver.js` — `solve(grid)` 重写：内部走 `min2phase.solve(gridToFacelet(grid))`，归一化多余空格、Error→抛错；**签名/输出不变**(仍 "U R2 F' …")。`selfTest` 改判 1..25 步。弃用 rubiks-cube-solver。
- `tests/solver.test.mjs` — 新增 6 用例(先红后绿)：① gridToFacelet(solved)=标准串 ② faceletToGrid 逆 ③ 50 随机态 grid↔facelet 互逆 且 solve(grid)==min2phase.solve(facelet)≤25 步 ④ 已还原 0 步 ⑤ SCRAMBLE_GRID 可解 1..25 ⑥ 记号合法无 prime。
**验证**：`node --test tests/` **8/8 PASS**(2 smoke+6 solver)；`selfTest` **19 步**；集成验证 `moves.js` 解析 min2phase 输出 0 异常→guide 不改、input 不改即可用。SCRAMBLE_GRID 经 min2phase 可解(无需重造)。
**遗留/下一步**：**T1.5 端侧实测 + 首解 init UX**——首次 solve 触发 ~1–4s 同步建表，input 的「求解中…」可能来不及刷新；待真机量实际冻屏、再决定 app 启动预热 vs 首解前先刷提示。Craft/真机端到端待用户验。

## [2026-06-18] T1.2/T1.3a：定离线 min2phase + vendored 验证 + 免烘焙
**背景/触发**：T1.1 数据支持离线；进 T1.3a——拿真 min2phase 源、验 QuickJS 可跑、量建表冻屏，定"烘焙二进制 vs 启动建表"。
**改了什么**：
- `lib/vendor/min2phase.js` — **新增**，vendored cs0x7f/cstimer `src/js/lib/min2phase.js`(Shuang Chen 对称约简实现，39KB)，IIFE+无 node/浏览器依赖，尾部加 `export default min2phase`(ESM)。公开 API：`solve(facelet)`/`randomCube`/`fromScramble`/`initFull`。
- `tests/min2phase.smoke.test.mjs` — **新增**，`node --test`：①已还原态 solve=0 步 ②50 随机态 往返(打乱+解=还原、步数≤25)。**全绿** → 证明 vendored 文件能按 App 方式 ESM import、无端侧不可用依赖、求解正确。
- `package.json` — 加 `"type":"module"`(对齐 App 把 .js 当 ESM，使 lib 可被 node --test 直接 import)与 `"test":"node --test tests/"`；测量工具改 `.cjs`(measure-kociemba/min2phase)、`.mjs`(measure-min2phase-build)。
**实测建表成本(vendored, node)**：首次 solve(部分初始化) 65ms、`initFull()` 40ms → 合计 ~105ms → 端侧 ×10–40 ≈ **1–4.2s 一次性**；内存 +3.1MB；稳态求解 avg 0.87ms / max 19ms。
**决策(T1.2/T1.3a)**：**离线 min2phase，免烘焙二进制**——建表仅 ~1–4s 一次性(远小于 cubejs 几十秒)，启动/首次求解时建一次表+「初始化中…」提示即可，省掉序列化/打包/改库加载的复杂度。烘焙仅作"真机建表>5s 才回退"的后备。
**验证**：smoke test 2/2 PASS；vendored 文件无 require/window/document/Buffer/process。真机一次性建表实际冻屏时长待 Craft/真机确认(估 1–4s)。
**遗留/下一步**：T1.4a——`cube.js` 加 `gridToFacelet`(URFDLB)、`solver.js#solve` 内部改走 min2phase(签名/归一化不变)，先写 `tests/solver.test.mjs` 再实现(TDD)。

## [2026-06-17] T1.1 完成：本地求解实测 → 离线 min2phase 可行
**背景/触发**：执行实现方案 T1.1——量近最优求解器的表大小/耗时/步数，为 T1.2 决策拿数据。用户要求"先测本地求解要多久"。
**改了什么**：
- 新增 `tools/measure-kociemba.js`（cube-solver 代理）与 `tools/measure-min2phase.js`（cstimer_module = Shuang Chen 真 min2phase）；devDependency 加 `cube-solver`、`cstimer_module`。
**实测（node，本机）**：
| | cube-solver(慢代理) | **min2phase(真)** |
|--|--|--|
| 单次求解 avg | 61ms | **2.3ms** |
| p95/max | 193/431ms | **4.3/25ms** |
| 内存 ΔheapUsed | +24MB | **+3.5MB** |
| 建表 | 994ms | **86ms** |
| 步数 avg | 21.6 | **20.1** |
- 正确性闸门(测试先行)：cube-solver 50/50 scramble+solution 还原、步数≤25 PASS。
- 端侧估算(×10–40)：min2phase 平均 23–91ms、p95 43–174ms、**最坏 ~0.25–1s**。
**结论（T1.2 倾向）**：**离线 min2phase 清晰可行**——长尾顾虑消除(最坏~1s)、内存 3.5MB 可容、步数 20。cube-solver 的 24MB/431ms 是非对称约简慢实现，非真实代表。建表仅 86ms→端侧 ~1–3.4s，故 T1.3a 可能"启动时建表一次(配初始化提示)"即可、未必需烘焙二进制——待真机测建表冻屏时长再定。
**验证**：两脚本 node 跑通；min2phase 输出 20 步随机态打乱(=求解长度)。无 App 代码变更(VERSION 仍 1.0.13)。
**遗留/下一步**：T1.2 拍板(数据支持离线为主)；T1.3a 先验 cstimer min2phase 源能否在 QuickJS 跑 + 真机建表冻屏时长 → 定"启动建表 vs 烘焙二进制"。

## [2026-06-17] 《开发实现方案》v2.2：TDD 双重验收 + 标核心
**背景/触发**：用户指定 **W1/W2 为项目核心**，且要按 **TDD** 开发——每个模块"开发中"和"开发完"都要有明确接受标准。
**改了什么**：
- `docs/魔方还原助手_开发实现方案.md` → **v2.2**：①新增 §2「开发范式：TDD + 双重验收」——纯逻辑模块(cube/solver/moves/colors/voicefix，无 wx、node 可 import)先写 `tests/*.test.mjs` 再实现(红→绿→重构)，UI/设备先定可观察清单；②卡片格式由"改动/依赖/验收"升级为 **改动/依赖/测试先行(开发中)/完成验收(开发完)**，每个 T 任务都补了测试先行用例(如 T1.4a 的 gridToFacelet 期望串、回放 isSolved、50 随机态、输出无 prime；T3.1 parseFix 用例；T2.3 真机样本集 classifyColor 断言)；③W1/W2 标★核心，§4 顺序与 §5 验证强调 `node --test tests/` 为主战场、先落测试再改实现。
**为什么这么改**：核心模块靠"先写测试定义完成"才能保正确性/识别率；双重验收让"开发中"也有可对的标准。
**验证**：文档类改动，无代码变更（VERSION 仍 1.0.13）。
**遗留/下一步**：进 T1.1 时先落 round-trip 自检，再写烘焙脚本——按 TDD 顺序推进。

## [2026-06-17] 重写《开发实现方案》→ 可执行任务分解(v2.1)
**背景/触发**：先重写为 v2.0(对齐 PRD/代码、删 cubejs/D-pad/selectComponent/localStorage/cube-coach 旧写法)；但用户评 v2.0「太抽象/被掏空/结构不对」，要的是**任务拆解+验收为主、不堆代码**。用 `doc-coauthoring` skill 流程（marketplace 搜确认它是唯一对口写技术规格的 skill）重做。
**改了什么**：
- `docs/魔方还原助手_开发实现方案.md` 重写为 **v2.1 执行清单**：§1 现状基线(已完成/勿重写 + lib 模块表 + 待补) → §2 任务卡片格式 → §3 任务分解(W1–W6 拆成 T1.1…T6.3，每张卡片三行：改动[点名文件/函数]+依赖+可测验收，不贴大段代码) → §4 推进顺序(ASCII 依赖图，W1 先行、W2/W3 并行) → §5 验证参考。
- Stage 3 用无上下文子 agent 做可执行性测试(评 "Mostly")，据反馈补实：T1.1 产出"表序列化格式说明"作 T1.3a 对接契约；T2.3 验收给数(≥8/9)；T4.1 列命令词表+绑定点；T6.3 用 SCRAMBLE_GRID 往返验证。
**为什么这么改**：实现方案要能照着开工，不是再写一遍 PRD；卡片化(改动/依赖/验收)是"可执行+可测"的最小载体。
**验证**：子 agent 可执行性测试 W1 主线+T3.1 判定可执行、结构清晰；定性验收项已补数/补细节。无代码变更（VERSION 仍 1.0.13）。
**遗留/下一步**：按 §4 顺序——先 T1.1 node 烘焙原型量数据，再过 T1.2 决策门。

## [2026-06-17] v1.0.13 全中文显示 + 滑动节流(真机灵敏度)
**背景/触发**：① 用户要求所有内容先用中文（颜色、求解步骤）；② 真机测试触控滑动太灵敏，一次滑动跳 2–3 格。
**改了什么**：
- 全中文显示（复用 `lib/cube.js` 已有 `COLOR_NAME_CN`/`FACE_NAME_CN`，内部仍存字母不影响求解逻辑）：
  - `pages/input` 色块显示 `白/黄/红/橙/绿/蓝`（render 加 `label`）；六面标签 `顶/右/前/底/左/后`→`顶面…后面`（去掉 U/R/F 字母）。
  - `pages/scan` review 网格色块显示中文色名。
  - `pages/guide` HUD 中央大字由面字母(U/R/F)改中文单字（取 `s.name[0]`=顶/底/前/后/左/右）；步骤文案本就中文（moves.js stepText「右面 顺时针」）。
- 滑动节流：三页 `onKeyDown` 仅对方向键(滑动)加 `throttleSlide()`，`SLIDE_COOLDOWN=250ms` 内的重复滑动忽略→一次滑动只走一格/一步；单击(Enter)不受影响。用 `performance.now()`（回退 Date.now）。
- VERSION 1.0.12→**1.0.13**。
**为什么这么改**：中文更直观（PRD 路线 E）；真机一次物理滑动连发多个方向事件，冷却时间收敛为一次。
**验证**：三页 `<script setup>` 抽出 `node --check` 通过。真机滑动手感/节流值(250ms)是否合适、中文显示是否清晰待用户验证。
**遗留/下一步**：据真机反馈微调 `SLIDE_COOLDOWN`（太钝则调小、仍跳格则调大）；求解内核近最优仍是最高优先。

## [2026-06-17] PRD 用 doc-coauthoring 流程重写为精简版
**背景/触发**：用户觉得 PRD 太乱，要找写 PRD 的 skill。
**改了什么**：
- 安装 `doc-coauthoring` skill 到 `~/.claude/skills/`（Anthropic 官方 agent-skills，明确支持 PRD/spec；下次会话可 `/doc-coauthoring` 调用）。
- 按其三阶段流程重写 `docs/魔方还原助手_MVP_产品设计.md`：定位=本人开发用、精简、技术放附录、中文。15 节正文→6 节正文(产品定义/硬件硬约束/功能范围/核心流程交互/关键设计决策/开发状态下一步)+6 附录(求解器对比/HUD/语音纠错话术/状态机/识别管线/能力映射)。现在时陈述，清除"相对v0.1/✅🚧❌/删除线"等变更痕迹（那些归开发日志）。
- Stage 3 用无上下文子 agent 做 Reader Testing，据反馈修三处：①双击=返回上一级(消除"列了双击却说不依赖"的矛盾)；②点明 min2phase≡Kociemba 两阶段实现；③语音纠错补"触发方式"(确认页直接说)。
**为什么这么改**：文档读者=自己/开发，无需对外营销章节；精简+附录分层让正文只讲"做什么+决策"。
**验证**：子 agent 读者测试通过(核心问题均可答)，三处矛盾/歧义已修；无代码变更(VERSION 仍 1.0.12)。
**遗留/下一步**：仍是求解内核——node 跑 min2phase 烘焙原型量表大小+耗时，再真机定离线/云端。

## [2026-06-17] 硬件/API 开发参考文档 + 求解器定向近最优(Kociemba 两阶段)
**背景/触发**：① 用户要求把眼镜硬件+软件 API 信息汇总成一份随时可查的开发参考；② 用户要"近最优+毫秒级"求解（CFOP 步数太长劝退），让我调研横向对比后把最优方案写进 PRD；③ 我此前在"硬件能力"上靠推断，这次系统读全了 KB + skills。
**改了什么**：
- 新增 `docs/硬件与API开发参考.md` — 以文档为准的全量开发参考（运行时/显示/交互/相机/Canvas/音频语音/AI/传感器/网络/存储/蓝牙/系统信息/生命周期/求解器约束/项目资源/坑位），标注 ✅文档证实/🧪实测/❓待测。关键实锤：QuickJS(2024-01-13) 无 Worker/无 WASM；相机仅 takePhoto 无视频流；无端侧 ML/NPU；HTTPS 经蓝牙→手机链路；`wx.getSystemInfo` 等存在。
- `docs/魔方还原助手_MVP_产品设计.md` — §4 重构为「近最优两阶段解(主)+LBL 教学(可选)」：主求解 CFOP→**Kociemba 两阶段 ~20 步**，实现两条路（离线烘焙 min2phase 为主 / 云端 fetch 为可选），LBL 降为可选模式；§8.1/§8.3 求解器、§10 北极星、§11 进度、§12-D 路线同步；§12-A「三面一拍」标注❌已验证放弃（真机广角+斜拍+反光，27 格不可靠）。
- 记忆：新增 `rokid-glasses-hardware-limits.md`（reference）并修正"getSystemInfo 不存在"的错误。
**为什么这么改**：硬件信息集中成一处便于后续做方案随时调用；近最优两阶段是本硬件下"步数短+可算"的最佳点，但运行时建表会冻屏，故必须离线烘焙或走云端。
**验证**：文档/记忆类改动，无代码变更（VERSION 仍 1.0.12）。求解器横向对比有 6 个来源。
**遗留/下一步**：跑 node 烘焙原型量出 min2phase 表大小(MB)+单次求解耗时 → 真机定"离线 vs 云端"主选项；离线/云端主选项仍待用户最终拍板（PRD 已记推荐：离线烘焙为主、云端为可选/兜底）。

## [2026-06-17] PRD：纠错拆为「手动 + 语音」两种
**背景/触发**：用户要求扫描确认页的纠错分两种——手动纠错(现有逐格切色)与**语音纠错**(用户说"白色面下方色块应该是蓝色""红色面左上方应该是绿色"，按 面+方位+目标色 改对应格)。
**改了什么**：
- `docs/魔方还原助手_MVP_产品设计.md` — §5 功能项 2 改为"两种方式并存"：手动(✅已实现) + 语音(🚧规划)，并定话术规范(面=颜色名/方位名；方位词表 左上/上/右上·左/中/右·左下/下/右下 对应九格；解析失败语音回询不盲改)；§6 流程、§12 路线 F 同步。
**为什么这么改**：语音纠错免手、更符合眼镜定位；先在文档定清话术规范再实现。
**验证**：文档类改动，无代码变更（VERSION 仍 1.0.12）。
**遗留/下一步**：实现语音纠错解析器(面/方位/颜色词→网格坐标)；依赖扫描方向决策落地后接入扫描确认页。

## [2026-06-17] v1.0.12 扫描页自诊断预览 + 修正对齐心智模型（视差/广角问题）
**背景/触发**：用户用真机眼镜拍照发现——① 三邻面一拍识别不可靠（魔方在广角画面里太小、斜拍透视+反光）；② **摄像头在眼镜左侧，与眼睛/显示不共线（视差）**，"显示中央画个框让用户对齐"单独做无效，因为框在视线坐标系、采样在摄像头坐标系。用户不确定真机上 `<camera>` 实时预览是否渲染。
**关键结论**：对齐框必须叠在**实时预览**上(摄像头坐标系)，用户对齐"预览里的魔方"而非眼前真实魔方，视差自动消失(手机魔方 App 与 scanner 样例均如此)。整套方案卡在"真机预览是否显示"这一未知上。
**改了什么**：
- `pages/scan/index.ink` — 预览下方加自诊断提示行「能看到实时画面=预览正常；全黑=预览不可用」，便于用户一眼确认；aim 提示改为「把魔方凑近镜头、让这一面填满下方绿框(对齐预览里的框)」纠正心智模型(广角→须凑近填满；对齐预览而非眼前)。
- `pages/input/index.ink` — VERSION 1.0.11→**1.0.12**。
**为什么这么改**：先用最小改动把"预览是否渲染"这个决定性未知变成用户可自查；并把对齐逻辑锚到摄像头坐标系。三邻面识别确认放弃。
**验证**：语法检查通过(改动为文案/样式)。真机预览是否渲染待用户确认。
**遗留/下一步**：据"预览是否渲染"分两条路——可见→叠框对齐预览(微调框大小/采样区)；不可见→改"拍广角照→自动检测魔方方块区→采样→人工纠错"。

## [2026-06-17] 产品文档升级为正式版（硬件无视频流，修订实时/AR 定位）
**背景/触发**：用户核实——**眼镜硬件不向应用开放实时视频流**，应用只能 `takePhoto` 取单帧。故"实时识别 / AR 精准配准"从"以后再做"变为"当前硬件不可行"；要求按此修订 MVP 产品文档，并结合后续迭代方向 + 当前开发进度升级为正式产品文档。
**改了什么**：
- `docs/魔方还原助手_MVP_产品设计.md` — 由 v0.1 草案重写为 **v1.0 正式版**（文件名保留以不破坏引用）：
  - 新增 §3「硬件能力与约束」：列明无视频流→只能拍照建初态+指令推演、无 AR 配准、单轴交互三条硬边界，作为全文档前提。
  - §2 定位修订：去掉"实时对位 / 箭头贴物理魔方"卖点，核心价值改为"解放双手 + 专属分步引导 + 语音免手"。
  - §4 教学法如实标注：目标 LBL，**当前实现为 CFOP（步数多、无阶段讲解）**；演示模式另备"最短解"开关。
  - §8 技术方案校正为实际 API/实现（`wx.media.createCameraContext`+`takePhoto`、webp/png/jpeg 分流、`wx` 跨页存储、绝对路由、`rubiks-cube-solver`、Canvas 在 onShow、单轴交互）。
  - §11 新增开发进度快照；§12 路线图按"受硬件限制 / 当前可做 / 远期"重新归类，纳入用户在原 §11 写的迭代笔记（适配不同三阶、三面一拍、最快解法、语音菜单栏、颜色中文名）。
**为什么这么改**：硬件边界是架构前提，必须前置写清并据此把"引导式扫描+确认"从权宜方案正名为既定主路径；文档要反映真实实现与进度，避免对外承诺不可实现的实时/AR 能力。
**验证**：文档类改动，无代码变更（故 VERSION 不变，仍 1.0.11）。
**遗留/下一步**：§12-D 求解内核升级（LBL 分阶段 + 演示最短解）为最高优先内核迭代项。

**背景/触发**：用户认为 App 核心是"实时掌握魔方状态"，要求调研现有视觉方案中识别物品状态的优秀方案。
**改了什么**：
- 新增 `docs/视觉方案调研_魔方状态识别.md` — 完整调研报告（经典 CV / YOLO 检测 / 视频流跟踪 / 6DoF 位姿 / 蓝牙智能魔方 五条路线 + Rokid 设备可行性评估）。
**为什么这么改**：核心结论——纯视觉实时全状态跟踪因贴纸旋转对称性是公认难题（OpenAI 也靠改造智能魔方）；主流 App（ASolver/CubeX）全是"扫 6 面建初态 + 指令推演更新状态"，与本项目现架构一致。建议优先做纯算法可行的读色强化：每面 9 格聚类 + 54 格全局一致性（每色恰 9 个）+ 中心块白平衡 + 连拍投票；引导页加阶段性复核做状态自校正。
**验证**：调研类，无代码改动；报告含全部来源链接。
**遗留/下一步**：按报告"建议路线"实施短期项（读色聚类+全局校验），可与已知待办"真机阈值校准"合并做。

## [2026-06-12] 代码托管到 GitHub
**背景/触发**：用户在 GitHub 新建仓库，需要把现有代码提交上去。
**改了什么**：
- 新增 `.gitignore` — 排除 `testImg/`（用户要求）、`AIUI/`（jsar-project/AIUI 的本地克隆，自带 .git，提交会变成损坏的子模块引用，需要时自行 clone）、`.claude/settings.local.json`、`node_modules/`。
- `git init -b main`，初始提交全部代码，与远端自动生成的 Initial commit（仅 2 行 README）合并（保留本地 README），推送到 `git@github.com:guiguihui/Rokid_Cube_Copilot.git`。
**验证**：`git push` 成功，远端 main 与本地一致；`git status` 干净。
**遗留/下一步**：以后改完代码记得 commit+push。

## [2026-06-06] v1.0.11 相机区域透明(排查实时预览不显示)
**背景/触发**：扫描页 `<camera>` 不显示实时画面(一直黑)。核查：`<camera>` 用法与官方 scanner 样例完全一致(无特殊属性，AGENTS 已声明 camera 权限)，**非代码结构问题**。实时画面由系统喂给 `<camera>`；疑似 Craft「上传图片」源不喂实时帧，或真机相机层被不透明背景遮挡。
**改了什么**：`pages/scan/index.ink` 把 `.wrap`/`.cam` 背景改 `transparent`，避免 webview 黑底盖住原生相机层。版本 1.0.11。
**验证**：语法检查通过。实时预览是否出现待用户按环境验证。
**遗留/下一步**：需用户确认环境(Craft/真机)与 Craft 摄像头源(上传图片/WebCam)；若官方 scanner 样例在其真机也黑，则为设备/权限问题非本项目。

## [2026-06-06] v1.0.10 首页右上角加版本号
**背景/触发**：真机看不到测试的是哪个版本。
**改了什么**：`pages/input/index.ink` 加 `VERSION='1.0.10'`，首页工具栏右端显示 `v{{version}}`。**约定：以后每次改代码递增 VERSION，并在回复里告知版本号。**
**验证**：input 语法检查通过。

## [2026-06-06] 扫描实时预览放大+九宫格对齐采样区
**背景/触发**：需要实时画面+九宫格叠加以对齐魔方、提高识别准确率。
**改了什么**：`pages/scan/index.ink` CSS——`<camera>` 预览 168→**240**px(正方形)，`.guide` 九宫格放到**中央 66%(158px@41,41)**并加 2px 外框、内格线加粗、字号 22 加粗；aim 提示改为「把这一面对齐填满九宫格 →拍照」。
**为什么这么改**：九宫格中央 66% 正好对应 `colors.js#sampleFace` 的采样区(min(w,h)*0.66 居中)，且预览正方形按 cover 居中裁剪→所见即所采，对齐后采到正确色块。实时预览/叠加结构本就有(`<camera>`+`.guide`)，此次主要放大加清晰。
**验证**：scan 语法检查通过。真机实时预览是否渲染、对齐是否精确待验。
**遗留/下一步**：若真机预览非 cover(而是 letterbox/拉伸)导致九宫格与采样错位，再按实际 fit 调整 guide 几何或 sampleFace 区域。

## [2026-06-06] 真机拍照「慢非错」：撤回诊断，加等待提示
**背景/触发**：真机点拍照曾报 `Exception generated by QuickJS`，经确认是**设备处理慢（webp 大图解码耗时），非功能 bug**。
**改了什么**：撤回上一步加的分阶段诊断，`doCapture` 还原为简洁版；仅在拍照中状态加提示「设备处理可能较慢，请稍候」。
**验证**：scan 语法检查通过。
**遗留/下一步**：如需可后续优化真机解码速度（降分辨率/采样区域）。

## [2026-06-06] 首页恢复改色(环形遍历)+ 不合法显示具体原因
**背景/触发**：扫描带回首页后求解失败仅显示笼统「状态不合法」；且需在首页修正误读，但之前去掉了首页改色、且旧导航有格子遍历不到。
**改了什么**：
- `pages/input/index.ink` — 恢复**首页逐格改色**：焦点序列 0..3=按钮、4..57=54 格，**环形单轴导航**(`focusNext/Prev` 用 `%NAV_LEN` 到尾绕回头)确保每个按钮/格子都能逐个到达；单击=按钮动作或切该格颜色(中心锁定跳过)；焦点格 `.foc` 高亮。
- 求解错误**显示具体原因**：`validate` 不合法→`'不合法：'+errs.join('；')`(如「白色有7个(应为9)；红色有10个(应为9)」)；数量都对但 solve 抛错→提示「六色数量各9但此状态无法还原(多半识别错位/朝向不符)」。
**为什么这么改**：硬件单轴+环形是覆盖全部色块且不遗漏的唯一可靠方式；具体计数原因让用户能定位改哪。
**验证**：input 语法检查通过、ink:for 7 处全平级；validate 具体消息 node 验证 OK。
**遗留/下一步**：扫描朝向/行序与解算一致性仍需真实可解状态验证。

## [2026-06-06] 修复求解页画布空白：onReady 不被支持
**背景/触发**：进引导页 canvas 全黑（只有底部 fallback 文字显示）。
**改了什么**：`pages/guide/index.ink` 把 `render()/speak()/startVoice()` 从 **`onReady`** 移到 **`onShow`**（Ink 生命周期只有 onLoad/onShow/onHide/onUnload，**无 onReady**，故绘制从未执行）；语音启动用 `_voiceStarted` 防重。
**为什么这么改**：canvas 样例也在 onShow 绘制；onReady 不会被调用。
**验证**：语法检查通过、onReady 已移除。Craft 端待验证箭头绘制。
**遗留/下一步**：若仍空白，排查 `wx.createCanvasContext('hud')` 是否返回 null（已 `if(!ctx)return`）。

## [2026-06-06] 一屏布局压缩 + 测试图片绕过摄像头
**背景/触发**：扫描页/求解页超出一屏（眼镜高上限 380px）；本机 webcam 颜色不准、暂无真机，但能上传图片到 Craft。
**改了什么**：
- `lib/testImages.js` — **新增**：`loadTestFrame(face)`→`{data,mimeType}`；优先用 `TEST_DATA`(base64/dataURI)，否则 `fetch(TEST_PATHS[face])`(默认 `/assets/test/<面>.jpg`)取 arrayBuffer。含自写 base64 解码(已对照 Buffer 验证逐字节正确)。
- `pages/scan/index.ink` — aim 模式加「测试图片」动作；`capture`重构为 `doCapture(source)` + `captureCam`(takePhoto)/`captureTest`(loadTestFrame)，共用 `decodeFrame`+`sampleFace`。布局压缩：相机 240→168、引导框 170→120、字号/间距收紧。
- `pages/guide/index.ink` — canvas 300→210 高、重算箭头/文字坐标；去掉不可点的按钮行；间距收紧 → 一屏内。
- `assets/test/README.md` — 上传 6 张面照片(U/R/F/D/L/B.jpg)的说明。
**为什么这么改**：AIUI 支持 `fetch`+`res.arrayBuffer()`，无 chooseImage/文件系统 API，故用 fetch 读项目内上传的图片绕过摄像头；fetch 不通则 base64 烘焙兜底。
**验证**：三处语法检查通过；base64 解码 cube.jpg 逐字节 PASS。Craft 端 fetch 资源是否可达待用户验证。
**遗留/下一步**：若 fetch('/assets/test/..') 在 Craft 读不到，改用 base64(我烘焙)。

## [2026-06-06] 改单轴线性交互 + 首页只展示 + 改色挪到扫描后
**背景/触发**：用户纠正——硬件只有 **向前/向后/单击/双击**（非上下左右四方向键），之前 2D 方向键导航致"色块没能全部选中"。且要求首页只展示、改色放到拍照后。
**改了什么**：
- `pages/input/index.ink` — **重写为首页只展示**：魔方两行各三面(U R F / D L B，6 个平级 face 块避免嵌套 ink:for)；仅 4 按钮可选；单轴线性导航 `focusNext/Prev`(±1) + `activate`；移除首页所有改色逻辑。加 `lastKey` 调试行。
- `pages/scan/index.ink` — review 模式**支持逐格改色**：`items()` 按模式给可选项列表(review=9格+接受+重拍；aim=拍照+上一面+退出)，`focusNext/Prev`+`activate`(cell→`nextColor`/接受→下一面/重拍/拍照/退出)；焦点高亮格子与动作；加 `lastKey`。
- `pages/guide/index.ink` — `onKeyDown` 改线性(向前/单击=下一步、向后=上一步)，加 `lastKey`。
- 三页：forward=ArrowDown/Right、back=ArrowUp/Left、confirm=Enter/Space/NumpadEnter（真实硬件 code 待 lastKey 确认）。所有动作均为可选项，**不依赖双击**。
**为什么这么改**：硬件是单轴(向前/向后)+确认，必须线性导航才能逐项到达；把动作做成可选项即可不用双击。改色按用户要求移到拍照后。`lastKey` 用于现场确认真实键码。
**验证**：三页语法检查通过；`ink:for` 共 9 处全单层(无嵌套)。Craft 端到端 + 真实键码待用户验证。
**遗留/下一步**：据 lastKey 反馈把 forward/back/单击/双击 映射到真实 code；双击可后续做快捷方式。

## [2026-06-06] 修复跨页数据传递：localStorage→wx 存储
**背景/触发**：扫描识别准确，但返回输入页后六面未更新（输入页仍是初始网格、焦点仍在「扫描魔方」=onShow 加载分支没生效）。
**改了什么**：
- 全部跨页存储 **Web `localStorage` → `wx.setStorageSync/getStorageSync`**（样例 `simple/pages/storage` 证实可用；Web localStorage 疑似 Ink 里不跨页共享）。
  - `pages/scan` finalize：`wx.setStorageSync('cube.scannedGrid', grid)`。
  - `pages/input`：新增 `applyScannedGrid()`，在 `onLoad` 与 `onShow` **双触发**读取(兜住 navigateBack 触发哪个生命周期)；读到则载入网格、焦点跳「求解」；用 `wx.removeStorageSync` 清除避免重复载入。加了 console.log 诊断。
  - `pages/input` doSolve：`wx.setStorageSync('cube.moves', moves)`；`pages/guide` onLoad：`wx.getStorageSync('cube.moves')`。
**为什么这么改**：`getCurrentPages/getApp` 文档未列(不可用)；wx 同步存储是样例验证过的跨页通道。双生命周期读取规避 navigateBack 是否触发 onShow 的不确定性。
**验证**：三页语法检查通过、无 localStorage 残留。Craft 端到端待用户验证(已加 `[input] applyScannedGrid: loaded/none` 日志便于定位)。
**遗留/下一步**：若仍不更新，看 Console 日志判断是 onShow 未触发还是 wx 存储未共享。

## [2026-06-06] 加 JPEG 解码器
**背景/触发**：确认 Craft takePhoto 返回 JPEG，需支持。
**改了什么**：
- `lib/vendor/jpeg-decoder.js` — **新增**，vendored jpeg-js 0.4.4 解码核心(notmasteryet, Apache-2.0, 无依赖, baseline+progressive)，UMD→ES(`export default decode`)。
- `lib/jpeg.js` — **新增**，`decodeJpeg(buf)`→`{width,height,rgba}`（`decode(buf,{useTArray:true,formatAsRGBA:true})` 避开 Node Buffer）；`isJpeg`。
- `pages/scan/index.ink` — `decodeFrame` 加 jpeg 分支(`isJpeg→decodeJpeg`)。
**为什么这么改**：jpeg-js 自洽、纯 JS、useTArray 不依赖 Buffer，适合 rquickjs。
**验证**：node——用 jpeg-js 编码器造魔方色 JPEG(q92)→vendored 解码器解→`sampleFace` 得 `WYROGBWYR` PASS，RGB 与原色近乎一致；全文件语法检查通过。
**遗留/下一步**：Craft 真实拍照端到端 + 颜色阈值真机校准。

## [2026-06-06] 修复扫描解码：Craft takePhoto 返回 PNG 非 WebP
**背景/触发**：扫描页拍照报 `Failed to read WebP features: bitstream-error, chunk=unknown`。Craft 的 `takePhoto` 返回 **webcam 截图(PNG/JPEG)，非 WebP**，而我只用了 `decodeWebP`。另有无害警告 `set_data: key=mode has no bindings`。
**改了什么**：
- `lib/vendor/tiny-inflate.js` — **新增**，自洽 DEFLATE 解压(foliojs tiny-inflate, MIT, 无依赖)，UMD→ES。
- `lib/png.js` — **新增**，极简 PNG 解码(8-bit, colorType 0/2/4/6, 非隔行) → `{width,height,rgba}`，用 tiny-inflate。
- `pages/scan/index.ink` — `decodeFrame(photo)` 按 magic 字节分支：webp→decodeWebP、png→decodePng、jpeg→提示待支持、未知→报 mimeType+magic；debug 行加显示 mimeType。`mode` 改为实例变量(不进 data)消除 `has no bindings` 警告。
**为什么这么改**：不同运行时 takePhoto 编码不同，需按真实格式解码；PNG 是浏览器 canvas 截图默认。tiny-inflate 自洽适合 rquickjs（不像 upng 依赖 pako）。
**验证**：node 合成 PNG（逐行轮换 0~4 号 filter，RGBA/RGB 两类）`decodePng` 往返**精确 PASS**；全文件语法检查通过。Craft 真实拍照待用户验证。
**遗留/下一步**：若 Craft 实为 JPEG，按屏幕提示的 mimeType 再加 jpeg 解码器。

## [2026-06-06] 实现摄像头扫描色块
**背景/触发**：要测试"扫描魔方获得每面色块"。此前 `lib/colors.js`/扫描页未实现。
**改了什么**：
- `lib/colors.js` — **新增**：`rgbToHsv` / `classifyColor`(6色) / `sampleFace`(中央引导区 3×3 采样、每格小块均值+HSV)；阈值集中在 `CONFIG`。
- `pages/scan/index.ink` — **新增**：`<camera>` 预览 + 3×3 引导框；D-pad 流程(确认=拍照→识别→显示9格字母+中心RGB/HSV调试；确认=接受并下一面；↑=重拍；←=上一面；返回退出)；6 面采到的网格中心强制为身份色后存 `localStorage['cube.scannedGrid']`，`navigateBack` 回输入页。
- `app.json` — pages 加 `pages/scan/index`。
- `pages/input/index.ink` — 工具栏加「扫描魔方」(焦点 0，其余顺延为 载入1/重置2/求解3，格子起始 `CELL_BASE=4`，`NAV_MAX=57`)；新增 `onShow` 读取并清除 `cube.scannedGrid`、载入网格、焦点跳「求解」、提示校正。
**为什么这么改**：摄像头取整帧像素(takePhoto→decodeWebP rgba)是一次性解码、不冻结；引导式逐面+确认+可回输入页校正，符合 PRD 且对 D-pad 友好。扫描结果中心强制身份色避免误读。
**验证**：node 离线——标准色分类 8/8 PASS、合成帧 `sampleFace` 得 `WYROGBWYR` PASS；全文件语法检查通过。真实相机准确率/阈值待 Craft 摄像头页或真机验证。
**遗留/下一步**：真实光照下校准 `CONFIG` 阈值；校验扫描朝向能否求解。

## [2026-06-06] 修复交互模型：改 D-pad + 修跳转路径 bug
**背景/触发**：界面能出来了，但**点击按钮/格子无反应**。用户截图「按键」面板显示 Rokid 眼镜只有 ↑↓←→ + 确认键，**不支持任意元素点选**。日志另有 `Navigate to failed ... pages/input/pages/guide/index`。
**改了什么**：
- `pages/input/index.ink` — 全面改为 **D-pad 焦点导航**：线性焦点(0=载入打乱,1=重置,2=求解,3..56=54格)，←→ 移焦、↑↓ 跨行、确认键(Enter/Space/NumpadEnter)激活；`onKeyDown` 驱动；`bindtap` 仅作鼠标补充。载入打乱后焦点自动跳到「求解」。焦点高亮用 `.foc` 反白样式。
- `pages/input/index.ink` — `wx.navigateTo` 路径由 `'pages/guide/index'` 改为**绝对路径 `'/pages/guide/index'`**（相对解析会拼成 `pages/input/pages/guide/index` 导致跳转失败，连带报 `Exported default must be an object`）。
- `pages/guide/index.ink` — `onKeyDown` 统一 D-pad：确认/→/↓ 下一步、← 上一步、↑ 重复、返回退出；提示文案同步。
**为什么这么改**：眼镜交互方案必须是方向键+确认，之前用 `bindtap` 当主交互是错的。绝对路径是 AIUI navigateTo 的正确写法。
**验证**：两页 `node --check` 通过；navigateTo 确认为绝对路径。真机/模拟器端到端待用户测。
**遗留/下一步**：确认键真实 keycode 待真机确认；其余见上方待办。

## [2026-06-06] 修复浏览器冻结：换 table-free 求解器 + 对齐样例结构
**背景/触发**：点「运行智能体」整页卡死。定位为 `app.js#onLaunch` 同步跑 cubejs `initSolver()`（Kociemba 百万级建表，node 实测 2s，浏览器 rquickjs 慢 10–40 倍 → 数十秒冻结主线程）。
**改了什么**：
- `lib/vendor/cube-solver.js` — **新增**，内置 `rubiks-cube-solver`(CFOP, MIT, table-free, ~15ms)，UMD→ES 模块适配（`(this,…)`→共享对象，取 `.default`）。
- `lib/vendor/cube.solve.js` — **删除**（cubejs 不再用）。
- `lib/cube.js` — 去掉 facelet 映射，改 `gridToRcs`(face 顺序 front,right,up,down,left,back，小写面字母)；新增烘焙常量 `SCRAMBLE_GRID`(离线 Kociemba 生成的合法态)与 `cloneGrid`。
- `lib/solver.js` — 重写：`solve(grid)`=gridToRcs→RCS→归一化(`prime`→`'`)；`scrambledGrid()` 返回烘焙网格深拷贝；删 `initSolver`。
- `app.js` — 改成样例那样只 `console.log`，移除 solver 预热。
- `app.json` — 删除样例没有的 `viewport`。
- `pages/input|guide/index.ink` — `<script def>` 精简到只剩 `navigationBarTitleText`（删 `description`/`schema`，避免被当成 in-chat 卡片）；输入页求解改 `solve(this.grid)`、去掉"首次5秒"提示。
**为什么这么改**：rquickjs 同步重计算会冻结整页；CFOP 求解器无需建表、毫秒级。结构对齐 scanner 样例以排除结构性问题。
**验证**：node 离线 selfTest 28ms PASS；用 cubejs 当裁判**交叉验证 3 个打乱 + 烘焙网格 isSolved=true**；全文件语法检查通过；无残留 cubejs/initSolver 引用。
**遗留/下一步**：当时仍以为靠点击交互（下一条已修）。

## [2026-06-06] 修复 Craft 运行目标：应用提升到仓库根
**背景/触发**：Craft「运行智能体」一直跑旧的 `test-agent`，不显示我们的应用。
**改了什么**：
- 把 `cube-coach/` 下的 `lib/ pages/ app.json app.js AGENTS.md README.md` **提升到 `E:\ROKIT` 根**，替换根目录原 test-agent 空骨架。
- 删除 `pages/index`(test-agent 占位页) 与 `cube-coach/` 子目录；`package.json` 名称 test-agent→cube-coach；`.aixignore` 加 `docs/`。
**为什么这么改**：ROKIT 根本身是个 AIUI agent，Craft 以根为工作区运行；子目录项目不会被当成入口。提升到根即对齐运行目标，且页面路由/导入路径不变。
**验证**：根 `app.json` 指向魔方应用；无 test-agent 残留引用；语法检查通过。

## [2026-06-06] 首版实现（cubejs 方案）
**背景/触发**：按《开发实现方案》落地 MVP。
**改了什么**：新建项目结构；内置 cubejs(Kociemba) 求解器；`lib/cube.js`(映射/校验)、`lib/solver.js`、`lib/moves.js`；`pages/input`(颜色网格)、`pages/guide`(canvas 箭头+语音)；复制 scanner 的 webp 解码器备用。
**为什么这么改**：混合策略先用成熟求解器保正确；辅助输入(可编辑网格)优先。
**验证**：node 离线 selfTest/映射/解析全 PASS。
**遗留/下一步**：后续暴露两大问题——浏览器冻结(cubejs 建表)与交互模型(误用点击)，见上方两条。

## [2026-06-06] 立项文档
**背景/触发**：确定方向"AI 眼镜辅助魔方还原学习"。
**改了什么**：
- `docs/魔方还原助手_MVP_产品设计.md` — 产品设计(纯新手 LBL 教学/引导式扫描+确认/精简 MVP)。
- `docs/魔方还原助手_开发实现方案.md` — 可照写代码的开发规格(API 速查/数据模型/页面规格/时间盒/验证)。
**为什么这么改**：先对齐产品与技术方案再动手。
